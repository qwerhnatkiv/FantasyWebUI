Read `AGENTS.md` in this directory before doing anything else. It is the actual instructions file for
this repo (shared with other agent tools, which is why it isn't named `CLAUDE.md`) — this file exists
only so a cold Claude Code session finds it automatically instead of starting with no context.
